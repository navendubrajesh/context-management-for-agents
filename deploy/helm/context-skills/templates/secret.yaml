apiVersion: v1
kind: Secret
metadata:
  name: {{ include "context-skills.fullname" . }}-secret
  labels:
    {{- include "context-skills.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Replace via --set-file or external secrets operator in production.
  CONTEXT_SKILLS_OIDC_HMAC_SECRET: "replace-me-at-least-32-characters-long"
