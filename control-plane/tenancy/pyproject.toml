[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "context-tenancy"
version = "1.0.0"
description = "Multi-tenant isolation for context-management-for-agents."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["context_tenancy*"]
